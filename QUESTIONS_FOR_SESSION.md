# Focused questions for the AWS teaching session

1. For a course project that already runs in Colab, is a SageMaker smoke test plus one working QA example enough for an AWS extension, or would you rather see the full 50-question rerun?
2. For a local 4-bit Qwen inference workflow, which GPU-backed SageMaker JupyterLab instance would you suggest as the smallest practical option?
3. If the same script runs on EC2 and SageMaker AI Studio, would you consider that the right level of AWS extension, rather than rebuilding it as a SageMaker-specific pipeline?
4. Would you recommend keeping the v5.2 retrieval + reranker + local generator together on one provider rather than splitting retrieval and generation across providers?
5. For the class, would you prefer screenshots and a short smoke-test log from SageMaker, or a full notebook rerun?
